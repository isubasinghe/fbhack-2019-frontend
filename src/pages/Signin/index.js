import React, { Component } from 'react';

import './signup.scss';

class Signin extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div />
    );
  }
}

export default Signin;